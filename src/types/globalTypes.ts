export interface ICountries {
  code: string,
  name: string,
  capital: string,
  population: number,
  area: number,
}

export interface ICurrUser {
  email: string;
  displayName: string;
  photoURL: string;
  uid: string;
  id: string;
}

// interface ICurrUserObj {
//   currUser: ICurrUser;
// }
